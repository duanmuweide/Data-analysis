# 犯罪数据分析项目方案（优化版）

## 📋 项目概述
本项目基于犯罪事件数据集进行大数据分析，使用Hadoop生态系统（Hive、HBase）进行数据存储和分析，实现版本化数据管理、实时查询和多维度统计分析。

**数据规模：**
- 原始数据集：约100万+条犯罪事件记录
- 版本化数据：分为3个版本（versionid=1/2/3），支持增量更新
- 数据特征：包含事件基本信息、时间信息、地理位置、犯罪类型、受害者数据等多维度属性

## 🔧 环境要求
- Hadoop 3.x
- Hive 3.x（支持ORC、事务、分区、分桶）
- HBase 2.x
- Python 3.x + pandas（用于数据清洗，白名单操作）
- DeBeaver（用于数据库管理和SQL执行）

## 📁 数据结构分析

### 1. 犯罪事件主数据表

**核心字段说明：**
- **事件标识类：** incident_id, offence_code, cr_number, nibrs_code
- **时间类：** dispatch_time, start_time, end_time, year, month, day
- **位置类：** block_address, city, state, zip_code, latitude, longitude, location
- **犯罪信息类：** crime_name1, crime_name2, crime_name3, victims
- **执法类：** police_district, agency, place, sector, beat, pra, district_num
- **版本控制类：** versionid

## 🗄️ 数据存储设计

### 1. Hive表设计

#### 表1: crime_incidents_external（外部表）
**用途：** 映射原始CSV数据，支持版本控制
**特性：** 直接读取CSV文件，包含versionid字段
```sql
CREATE EXTERNAL TABLE IF NOT EXISTS crime_incidents_external (
    incident_id STRING, offence_code STRING, cr_number STRING,
    dispatch_time TIMESTAMP, start_time TIMESTAMP, end_time TIMESTAMP,
    nibrs_code STRING, victims INT, crime_name1 STRING, crime_name2 STRING, crime_name3 STRING,
    police_district STRING, block_address STRING, city STRING, state STRING, zip_code STRING,
    agency STRING, place STRING, sector STRING, beat STRING, pra STRING,
    address_num STRING, street_prefix STRING, street_name STRING, street_suffix STRING, street_type STRING,
    latitude DOUBLE, longitude DOUBLE, district_num STRING, location STRING,
    versionid INT -- 版本控制字段
)
ROW FORMAT DELIMITED 
FIELDS TERMINATED BY ',' 
STORED AS TEXTFILE 
LOCATION '/user/hadoop/crime_data/'
TBLPROPERTIES ("skip.header.line.count"="1");
```

#### 表2: crime_incidents（内部表）
**用途：** 用于数据处理和分析，性能优化
**特性：** 分区、分桶、ORC格式、事务支持
```sql
CREATE TABLE IF NOT EXISTS crime_incidents (
    incident_id STRING, offence_code STRING, cr_number STRING,
    dispatch_time TIMESTAMP, start_time TIMESTAMP, end_time TIMESTAMP,
    nibrs_code STRING, victims INT, crime_name1 STRING, crime_name2 STRING, crime_name3 STRING,
    police_district STRING, block_address STRING, city STRING, state STRING, zip_code STRING,
    agency STRING, place STRING, sector STRING, beat STRING, pra STRING,
    address_num STRING, street_prefix STRING, street_name STRING, street_suffix STRING, street_type STRING,
    latitude DOUBLE, longitude DOUBLE, district_num STRING, location STRING,
    versionid INT
)
PARTITIONED BY (year INT, month INT, day INT) -- 时间分区
CLUSTERED BY (city) INTO 4 BUCKETS -- 按城市分桶
STORED AS ORC -- ORC格式存储
TBLPROPERTIES (
    "transactional"="true" -- 事务支持
);
```

### 2. HBase表设计

#### 表名: crime_data_hbase
**用途：** 支持实时查询和版本化数据存储
**特性：** 5个列族，优化的RowKey设计

**RowKey设计：** `city:year-month-day:incident_id`
- 城市作为前缀：分散数据，避免热点
- 时间戳：便于按时间范围查询
- 事件ID：确保唯一性

**列族设计：**
```
create 'crime_data_hbase',
  {NAME => 'incident', VERSIONS => 1},  -- 事件基本信息
  {NAME => 'time', VERSIONS => 1},       -- 时间信息
  {NAME => 'location', VERSIONS => 1},   -- 位置信息
  {NAME => 'police', VERSIONS => 1},     -- 执法相关信息
  {NAME => 'version', VERSIONS => 1}     -- 版本控制信息
```

**列族字段映射：**
- **incident:** incident_id, offence_code, cr_number, nibrs_code, victims, crime_name1, crime_name2, crime_name3
- **time:** dispatch_time, start_time, end_time
- **location:** block_address, city, state, zip_code, latitude, longitude, address_num, street_prefix, street_name, street_suffix, street_type
- **police:** police_district, agency, place, sector, beat, pra, district_num
- **version:** versionid

## 📊 8个分析维度设计

### 维度1: 城市犯罪类型分布分析（Hive）
**分析内容：** 分析各城市主要犯罪类型的分布情况
**聚合函数：** COUNT, RANK
```sql
CREATE TABLE crime_type_distribution_by_city AS
SELECT
    city,
    crime_name1,
    COUNT(*) AS crime_count,
    RANK() OVER (PARTITION BY city ORDER BY COUNT(*) DESC) AS rank_in_city,
    versionid
FROM crime_incidents
WHERE versionid = 3 -- 使用最新版本
GROUP BY city, crime_name1, versionid;
```

### 维度2: 犯罪时间趋势分析（Hive）
**分析内容：** 分析犯罪事件随时间的变化趋势，按年/月/日统计
**聚合函数：** COUNT, AVG
```sql
CREATE TABLE crime_time_trend AS
SELECT
    year,
    month,
    COUNT(*) AS monthly_crime_count,
    AVG(victims) AS avg_victims_per_crime,
    versionid
FROM crime_incidents
GROUP BY year, month, versionid
ORDER BY year, month;
```

### 维度3: 犯罪地点类型分析（Hive）
**分析内容：** 分析不同地点类型的犯罪发生率
**聚合函数：** COUNT, PERCENT
```sql
CREATE TABLE crime_location_type_analysis AS
SELECT
    place,
    COUNT(*) AS crime_count,
    COUNT(*) * 100.0 / SUM(COUNT(*)) OVER () AS percentage,
    AVG(victims) AS avg_victims,
    versionid
FROM crime_incidents
WHERE versionid = 3
GROUP BY place, versionid
ORDER BY crime_count DESC;
```

### 维度4: 版本间数据变化分析（Hive）
**分析内容：** 比较不同版本间的犯罪数据变化
**聚合函数：** COUNT, SUM, DIFF
```sql
CREATE TABLE crime_version_comparison AS
SELECT
    city,
    crime_name1,
    COALESCE(SUM(CASE WHEN versionid = 1 THEN 1 ELSE 0 END), 0) AS v1_count,
    COALESCE(SUM(CASE WHEN versionid = 2 THEN 1 ELSE 0 END), 0) AS v2_count,
    COALESCE(SUM(CASE WHEN versionid = 3 THEN 1 ELSE 0 END), 0) AS v3_count,
    (COALESCE(SUM(CASE WHEN versionid = 3 THEN 1 ELSE 0 END), 0) - 
     COALESCE(SUM(CASE WHEN versionid = 1 THEN 1 ELSE 0 END), 0)) AS total_change
FROM crime_incidents
GROUP BY city, crime_name1
HAVING v3_count > 0
ORDER BY total_change DESC;
```

### 维度5: 受害者数量分布分析（Hive）
**分析内容：** 分析犯罪事件中受害者数量的分布情况
**聚合函数：** COUNT, SUM, PERCENT
```sql
CREATE TABLE victim_distribution_analysis AS
SELECT
    victims,
    COUNT(*) AS crime_count,
    COUNT(*) * 100.0 / SUM(COUNT(*)) OVER () AS percentage,
    SUM(CASE WHEN city = 'BALTIMORE' THEN 1 ELSE 0 END) AS baltimore_count,
    versionid
FROM crime_incidents
WHERE versionid = 3 AND victims IS NOT NULL
GROUP BY victims, versionid
ORDER BY victims;
```

### 维度6: 警区犯罪率分析（Hive）
**分析内容：** 分析不同警区的犯罪发生率和犯罪类型分布
**聚合函数：** COUNT, RANK
```sql
CREATE TABLE police_district_analysis AS
SELECT
    police_district,
    city,
    COUNT(*) AS total_crimes,
    RANK() OVER (ORDER BY COUNT(*) DESC) AS district_rank,
    COUNT(DISTINCT crime_name1) AS unique_crime_types,
    AVG(victims) AS avg_victims,
    versionid
FROM crime_incidents
WHERE versionid = 3 AND police_district IS NOT NULL
GROUP BY police_district, city, versionid;
```

### 维度7: 城市犯罪热点分析（HBase）
**分析内容：** 基于地理位置快速查询特定城市的犯罪热点
**查询方式：** HBase Shell/API
```bash
# 查询特定城市的犯罪事件
scan 'crime_data_hbase', {
  STARTROW => 'BALTIMORE:2023-01-01',
  STOPROW => 'BALTIMORE:2023-12-31',
  COLUMNS => ['incident:crime_name1', 'location:zip_code', 'time:dispatch_time']
}
```

### 维度8: 实时犯罪事件查询（HBase）
**分析内容：** 基于RowKey快速查询特定事件的详细信息
**查询方式：** HBase Shell/API
```bash
# 查询特定事件的完整信息
get 'crime_data_hbase', 'BALTIMORE:2023-12-09:1234567'
```

## 🔄 数据导入流程

### 步骤1: 数据清洗（Python，白名单操作）
```bash
cd data/
python clean.py
# 生成清洗后的数据文件 crime_cleaned.csv
```

### 步骤2: 版本化数据生成
```bash
# 将清洗后的数据分为3个版本
# versionid=1: 基础数据集
# versionid=2: 增加新数据
# versionid=3: 最新版本数据
# 生成文件: crime_version1.csv, crime_version2.csv, crime_version3.csv
```

### 步骤3: 上传数据到HDFS
```bash
hdfs dfs -mkdir -p /user/hadoop/crime_data/
hdfs dfs -put crime_version*.csv /user/hadoop/crime_data/
```

### 步骤4: 创建Hive表并加载数据
```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS crime_analysis;
USE crime_analysis;

-- 创建外部表
-- （外部表创建SQL见Hive表设计部分）

-- 加载数据到外部表（自动包含versionid）
LOAD DATA INPATH '/user/hadoop/crime_data/' INTO TABLE crime_incidents_external;

-- 加载版本1数据到内部表
INSERT OVERWRITE TABLE crime_incidents PARTITION(year, month, day)
SELECT *, YEAR(dispatch_time), MONTH(dispatch_time), DAY(dispatch_time)
FROM crime_incidents_external WHERE versionid = 1;

-- 增量加载版本2数据
INSERT INTO TABLE crime_incidents PARTITION(year, month, day)
SELECT *, YEAR(dispatch_time), MONTH(dispatch_time), DAY(dispatch_time)
FROM crime_incidents_external WHERE versionid = 2;

-- 增量加载版本3数据
INSERT INTO TABLE crime_incidents PARTITION(year, month, day)
SELECT *, YEAR(dispatch_time), MONTH(dispatch_time), DAY(dispatch_time)
FROM crime_incidents_external WHERE versionid = 3;
```

### 步骤5: 创建HBase表并导入数据
```bash
# 创建HBase表
hbase shell
create 'crime_data_hbase',
  {NAME => 'incident', VERSIONS => 1},
  {NAME => 'time', VERSIONS => 1},
  {NAME => 'location', VERSIONS => 1},
  {NAME => 'police', VERSIONS => 1},
  {NAME => 'version', VERSIONS => 1}
```

```sql
-- 创建Hive-HBase集成表
CREATE EXTERNAL TABLE crime_incidents_hbase (
    rowkey STRING,
    -- incident列族字段
    incident_id STRING, offence_code STRING, cr_number STRING,
    nibrs_code STRING, victims INT, crime_name1 STRING, crime_name2 STRING, crime_name3 STRING,
    -- time列族字段
    dispatch_time TIMESTAMP, start_time TIMESTAMP, end_time TIMESTAMP,
    -- location列族字段
    block_address STRING, city STRING, state STRING, zip_code STRING,
    latitude DOUBLE, longitude DOUBLE, address_num STRING,
    street_prefix STRING, street_name STRING, street_suffix STRING, street_type STRING,
    -- police列族字段
    police_district STRING, agency STRING, place STRING,
    sector STRING, beat STRING, pra STRING, district_num STRING,
    -- version列族字段
    versionid INT
)
STORED BY 'org.apache.hadoop.hive.hbase.HBaseStorageHandler'
WITH SERDEPROPERTIES (
    "hbase.columns.mapping" = ":key,incident:incident_id,incident:offence_code,incident:cr_number,incident:nibrs_code,incident:victims,incident:crime_name1,incident:crime_name2,incident:crime_name3,time:dispatch_time,time:start_time,time:end_time,location:block_address,location:city,location:state,location:zip_code,location:latitude,location:longitude,location:address_num,location:street_prefix,location:street_name,location:street_suffix,location:street_type,police:police_district,police:agency,police:place,police:sector,police:beat,police:pra,police:district_num,version:versionid"
)
TBLPROPERTIES ("hbase.table.name" = "crime_data_hbase");

-- 从Hive内部表导入数据到HBase
INSERT INTO TABLE crime_incidents_hbase
SELECT
    CONCAT(city, ':', DATE_FORMAT(dispatch_time, 'yyyy-MM-dd'), ':', incident_id) AS rowkey,
    incident_id, offence_code, cr_number, nibrs_code, victims, crime_name1, crime_name2, crime_name3,
    dispatch_time, start_time, end_time,
    block_address, city, state, zip_code, latitude, longitude, address_num,
    street_prefix, street_name, street_suffix, street_type,
    police_district, agency, place, sector, beat, pra, district_num,
    versionid
FROM crime_incidents;
```

## 📈 可视化方案

### 推荐工具
1. **Tableau** - 交互式地理空间可视化和时间趋势分析
2. **Power BI** - 企业级报表和仪表盘
3. **Python + Matplotlib/Seaborn** - 自定义图表和深度分析

### 核心可视化内容
1. **城市犯罪热图** - 基于地理位置的犯罪密度可视化
2. **犯罪类型分布饼图** - 各城市主要犯罪类型占比
3. **犯罪时间趋势图** - 按年/月/日的犯罪数量变化
4. **版本数据对比柱状图** - 不同版本间的犯罪数据变化
5. **受害者数量分布直方图** - 犯罪事件中受害者数量分布
6. **警区犯罪率排名图** - 各警区犯罪率对比

## ✅ 项目验证清单

- [ ] 数据清洗脚本执行成功，生成清洗后的数据
- [ ] 版本化数据（3个版本）生成完成
- [ ] Hive外部表和内部表创建成功
- [ ] 内部表支持分区、分桶、ORC格式、事务
- [ ] HBase表创建成功，包含5个列族
- [ ] Hive-HBase集成表创建成功
- [ ] 数据成功从Hive导入HBase
- [ ] 8个分析维度的查询执行成功
- [ ] HBase实时查询功能验证通过
- [ ] DeBeaver连接Hive和HBase成功
- [ ] 可视化图表生成完成

## 📝 项目特点

### 技术亮点
1. **版本化数据管理**：实现数据的增量更新和版本控制，支持基于versionid的查询
2. **高性能存储设计**：
   - Hive：ORC格式+Snappy压缩，分区+分桶优化
   - HBase：5个列族设计，优化的RowKey结构
3. **实时查询能力**：HBase提供毫秒级的事件详情查询
4. **多维度分析**：8个分析维度覆盖城市、时间、类型、位置等多方面
5. **数据清洗自动化**：Python脚本实现数据清洗流程（白名单操作）
6. **集成化管理**：DeBeaver支持Hive和HBase的统一管理

### 业务价值
1. **犯罪趋势预测**：基于历史数据预测未来犯罪趋势
2. **资源优化配置**：根据犯罪热点和时间分布优化警力部署
3. **政策制定支持**：为犯罪预防政策提供数据支撑
4. **公众安全提升**：通过数据分析提升城市安全水平
5. **版本化决策支持**：追踪数据变化，支持决策演进

### 优化亮点
1. **简化架构**：移除非必要组件，专注核心功能
2. **白名单合规**：仅使用白名单内的Python数据清洗功能
3. **版本控制**：完善的数据版本管理机制
4. **查询优化**：分区、分桶、RowKey优化提升查询性能
5. **可视化友好**：支持主流可视化工具，便于结果展示
6. **文档完善**：详细的操作指南和技术文档

## 📄 相关文档
- DeBeaver连接指南：`DeBeaver连接指南.md`
- 数据字典：`数据字典.md`
- Hive完整流程脚本：`hive_full_process.sql`
- Hive-HBase集成脚本：`hive_hbase_integration.sql`
- HBase表创建脚本：`hbase表创建.txt`
- 数据清洗脚本：`data/clean.py`
